window.SkewFlagEnglish = React.createClass({
  render: function() {
    var images = this.props.images;
    return(
      <div className="limited-wrapper -flex-center">
        <section className="inner-content">
          <div className="abstract-banner-content">
            <TransactionContainer type='blur' />
            <HideCoinAnimation />
            <h2 className="tag-text -banner -lightgrey -midsize">
              Let your patria level get to the sky
            </h2>
            <div className="pure-u-1 buttons-container">
              <a className="main-button -blue" href={this.props.blueButtonLink} >Get Chavezcoin</a>
              <a className="main-button -red" href="#">Download Wallet</a>
            </div>
          </div>
          <HeaderCount price={this.props.chcprice} />
          <ActionMobileEnglish images={images} />
        </section>
      </div>
    )
  }
})


window.HeaderHomeEnglish = React.createClass({
  render: function() {
    var images = $.parseJSON(this.props.images);
    return(
      <header className="pure-u-1">
        <section className="abstract-banner pure-u-1" id="inicio">
          <div className="pure-u-1 skew-container-abstract -flag-color">
          </div>
          <SkewFlagEnglish images={images} chcprice='1' blueButtonLink                                                                                                                                                                                                                                                                            ={this.props.blueButtonLink} />
        </section>
      </header>
    )
  }
})