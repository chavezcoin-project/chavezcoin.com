window.MarketsListEnglish = React.createClass({
  render: function() {
    var market = this.props.marketlist.markets;
    var percentVolume = this.props.marketlist.volume;
    var time = this.props.time;
    return(
      <div className="limited-wrapper market-list -flex-center">
        <table className=" pure-u-23-24 pure-u-md-4-5 table-list -market">
          <thead className="head">
            <tr className="data-wrapper">
              <th className=" data -mid">Exchange Name</th>
              <th className=" data -hide pure-u-1-5">Last Update</th>
              <th className=" data -mid">24h Volumen $USD</th>
              <th className=" data -small-big">Price $USD</th>
              <th className=" data -hide -small">Graphic</th>
            </tr>
          </thead>
          <tbody className="body">
            <MarketDetails key={market.market} name={market.market} price={market.price} volume={market.volume} total={percentVolume} time={time} />
          </tbody>
        </table>
      </div>
    )
  }
})